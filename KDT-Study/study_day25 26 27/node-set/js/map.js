// @ts-check
/*

const arr = [10, 20, 30, 40, 50];
// find 함수: 제일 처음 true가 되는 값을 차고 끝난다.
const result = arr.find(function(element) {
return element === 20;
});

console.log(result);

arr.map(function(element, index){
console.log(index+'번 값은' + element);
})
//map은  배열에 있는 값을 한개씩 호출해서 무언가를 시킬 때 사용하는 함수이다.

arr.map((element, index) => {
  console.log(index + '번 값은' + element);
})
*/
/*
for(let i = 0; i < arr.length; i++){
    console.log(arr[i]);
}

for (let item of arr) {
    console.log(item);
}
*/
